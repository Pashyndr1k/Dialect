{
  "name": "@dialect/cli",
  "version": "0.0.1",
  "type": "module",
  "description": "Compile a Prompt IR document into one model's dialect from the command line",
  "bin": {
    "dialect": "./src/main.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@dialect/core": "0.0.1",
    "@dialect/providers": "0.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}

[package]
name = "dialect"
version = "0.37.0"
description = "Compiles ideas and references into prompts written in each model's own dialect"
authors = ["Dialect"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "dialect_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
keyring = { version = "4.1.6", features = ["apple-native-keyring-store"] }
reqwest = { version = "0.13.4", features = ["json", "multipart"] }
tauri-plugin-dialog = "2.7.2"
# Updating itself, so a version that is out is a version people leave.
tauri-plugin-updater = "2"
# Restarting into the new version, when the person says so.
tauri-plugin-process = "2"
base64 = "0.23.1"

[dev-dependencies]
tokio = { version = "1.53.1", features = ["rt-multi-thread", "macros"] }

